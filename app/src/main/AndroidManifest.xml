<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.lyw.com.longrouter">

    <!--android:roundIcon="@mipmap/ic_launcher_round"-->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:taskAffinity="example.lyw.com.longrouter">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".filter.JumpActivity"-->
            <!--android:theme="@android:style/Theme.Translucent">-->
        <!--</activity>-->

        <activity android:name=".test.Test1Activity" />
        <activity android:name=".test.Test2Activity" />
        <activity android:name=".test.Test3Activity" />
        <activity android:name=".test.Test4Activity" />
        <activity android:name=".test.Test5Activity"/>
        <activity android:name=".test.Test6Activity"/>
        <activity android:name=".test.Test7Activity"/>
    </application>

</manifest>